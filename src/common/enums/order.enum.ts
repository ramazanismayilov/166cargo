export enum OrderStatus {
    PENDING = 'PENDING',
    SHIPPED = 'SHIPPED',
    IN_TRANSIT = 'IN TRANSIT',
    DELIVERED = 'DELIVERED',
}

export enum OrderCurrency {
    TRY = 'TRY',
    USD = 'USD',
}

export enum OrderCountry {
    TURKEY = 'TURKEY',
    USA = 'USA',
}
